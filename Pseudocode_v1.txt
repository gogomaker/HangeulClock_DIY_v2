각종 변수 선언...
void setup() {...}

void loop() {
    switch(모드) {
        case 시간모드:
            시간표시();
            break;
        case 온도모드:
            온도표시();
            break;
        case 습도모드:
            습도표시();
            break;
    }

    if(모드전환버튼 눌렸다가 때졌다면) {
        모드 전환(다음 모드로 전환);
    }

    if(LED버튼 짧게 눌렸다면) {
        밝기값 조절(밝기값은 일정하게 늘림)
    }
    if(LED버튼 길게 눌렸다면) {
        LED색상조정
    }
    if(시간 버튼 짧게 눌렀다면) {
        ...
    }
}